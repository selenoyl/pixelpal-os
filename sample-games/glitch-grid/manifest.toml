id = "glitch-grid"
name = "Glitch Grid"
version = "0.1.0"
exec = "bin/glitchgrid"
icon = "icon.ppm"
splash = "splash.ppm"
sdk_version = "1"
author = "PixelPal OS"
description = "Scan a digital grid, reveal safe sectors, and avoid corrupted nodes."
supports_network_lan = false
min_os_version = "0.1.0"
