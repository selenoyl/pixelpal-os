# Theme Audio

This folder holds launcher audio assets for the default PixelPal theme.

Expected files:

- `menu_theme.wav` for looping menu music
- `menu_move.wav` for D-pad navigation
- `menu_confirm.wav` for accept and launch actions
- `menu_back.wav` for cancel and back actions

WAV is the current supported format for launcher playback.

